/* tslint:disable */
/* eslint-disable */

/* auto-generated by NAPI-RS */

export interface QuoteParams {
  /** Operator anchor sqrt-price (Q64.96 uint160), decimal or 0x-prefixed hex. */
  sqrtPriceX96: string
  /** Current Y -> X fee in Q24. */
  feeAskX24: number
  /** Current X -> Y fee in Q24. */
  feeBidX24: number
  /** Active token-X reserve (uint112), decimal or hex. */
  reserveX: string
  /** Active token-Y reserve (uint112), decimal or hex. */
  reserveY: string
  /** Maximum immediate directional punishment in Q24. Defaults to disabled. */
  maxPunishmentX24?: number
  /** Exact input amount (uint256), decimal or hex. */
  amountIn: string
  /** Caller-specific fee multiplier (uint256). Defaults to one. */
  feeMultiplier?: string
}
export interface QuoteResult {
  amountOut: string
  /** Always equals the operator anchor in the linear implementation. */
  sqrtPriceNext: string
  fee: string
  /** Saturating directional fee used by this quote before feeMultiplier. */
  effectiveFeeX24: number
}
export enum SwapSimulationStatus {
  Applied = 'applied',
  SwapImpossible = 'swapImpossible',
  ReserveTransitionOverflow = 'reserveTransitionOverflow',
  LaterRevert = 'laterRevert'
}
export interface SwapSimulationResult {
  amountOut: string
  sqrtPriceNext: string
  fee: string
  /** Saturating directional fee used by the current quote before multiplier. */
  effectiveFeeX24: number
  /**
   * True when the pure-math quote is nonzero and the standard-token uint112
   * reserve transition fits. It does not predict external call failures.
   */
  executable: boolean
  /**
   * Pure-model outcome. Applied does not guarantee an on-chain transaction
   * cannot later revert during token transfer or accounting.
   */
  status: SwapSimulationStatus
  desiredPunishmentX24: number
  appliedPunishmentX24: number
  feeAskX24After: number
  feeBidX24After: number
  reserveXAfter: string
  reserveYAfter: string
}
export declare function quoteXToY(params: QuoteParams): QuoteResult
export declare function quoteYToX(params: QuoteParams): QuoteResult
export declare function simulateXToY(params: QuoteParams): SwapSimulationResult
export declare function simulateYToX(params: QuoteParams): SwapSimulationResult
export declare function priceToSqrtPriceX96(price: number): string
export declare function price_to_sqrt_price_x96(price: number): string
export declare function sqrtPriceX96ToPrice(value: string): number
export declare function sqrt_price_x96_to_price(value: string): number
